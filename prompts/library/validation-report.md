# Validation Report Prompt

Use after validation commands run.

```text
Command:
Result:
Automated coverage:
Manual coverage:
Known gaps:
```

Include exact command, exit status, important HEMTT output, and any manual SQA observations separately.
